

# Bucket page size
bucket.pagesize=1024

# Bucket pool size
bucket.poolsize=128

# Bucket compression schema
bucket.compression=SNAPPY

# S3 keys
#s3.access.key=
#s3.secret.key

#HDFS replication
#hdfs.replication = -1